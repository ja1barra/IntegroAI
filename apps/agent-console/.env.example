NEXT_PUBLIC_SUPABASE_URL=https://pjptveelaqivsmarzbii.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Shared secret required on every /api/* route in this app (they use the
# Supabase service-role client, which bypasses RLS). Send it as
# `Authorization: Bearer <key>` (or `x-api-key: <key>`) on all requests,
# including n8n's webhook config.
AGENT_CONSOLE_API_KEY=your_random_shared_secret_here
